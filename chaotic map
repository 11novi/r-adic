import numpy as np
import matplotlib.pyplot as plt
%matplotlib inline

#r-adic map function
def radic(r, x):
    return r * x % 1
x = np.linspace(0, 1)
fig, ax = plt.subplots(1, 1)
ax.plot(x, radic(2.999, x), 'k') #r=3 but should be not exactly 3
